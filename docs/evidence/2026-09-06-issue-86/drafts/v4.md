## Resolution - draft for #86 (Billy, 2026-09-06) - NOT RULED until posted

**What this comment is.** The four things the body asked for, under the definition `0101` landed: the necessity test's formalisation (§1), the two type questions the first application left open, plus two it did not name (§2), which judgments the coordinator must be able to make (§3) and what they make ring 0's standing intent and its rows (§4, §5), and whether crossing a Ref-typed field from a computed set and filtering on a link's own fields have a must-judgment (§8). Plus the two things the body carried in: the plan's scoping point (§0) and the `0042` overturn #82 authorised (§6, §7). §10 lists the amendments; Billy's posting of this comment is what rules them. Everything here is a draft until then, per `docs/agents/drafts-and-rulings.md`.

**Rulings Billy made in the session, recorded here for the first time.** Seven. The plan's representation stays at #14 and this ticket decides only which reads its inputs require (§0). Deciding what to do next across five courses needs an obligation's size and the owner's absorption; neither is a form; each is an owner-authored observation whose record's shape is parked whole; until that record exists absorption reaches the coordinator by asking, which is dispatch, and how size reaches it is open, because the only answer `0037` allows is a comparison between two members (§6). Ring 0's rows carry one field set, so `0042`'s partition dissolves and its three triggers become the coordinator's judgment rule (§7). The standing intent is a path from every obligation to its progress record, with `course` a Ref-typed field on the row and the term unconstrained, one term being an assumption the width bet carries (§4). Arrangement goes to #82 with three inputs (§9). #14's three-bucket evidence bounds the must-list and is not a source of it, so *which items need Billy* is a candidate and not a must (§3). A dangling `course` ref on an obligation is a defect the design tolerates and the standing intent keeps rather than drops (§4). Everything else below is derived from those and from landed records, and says which.

**How it was reached, including what the reviews changed.** By grilling, from the judgments outward: the formalisation and the type questions first, because nothing else could be run through the test before them; the must-list from ruled sources, with Billy's raw decomposition and #85's scenarios treated as candidates to test; the standing intent and its rows derived from the must-list under `0101`'s criterion. Two drafts went to blind review under two lenses each, and the reviews changed the substance twice. The first draft's standing intent was a three-position path from every `course`, its third position designed to carry `state` and checked against nothing else; review showed it silently lost an obligation whose `course` ref dangles, ran the criterion over the middle position and never the endpoint, placed J0's inputs in a record the intent could not reach, and omitted a judgment `0001`'s requirements sentence names. The second draft started from `obligation` but added a term predicate as a chain over `course`, which dropped the same obligation again, and stated a reachability constraint on the observation's record that read `0101`'s one-return premise for per-member observations, which are `0039`'s, and pre-chose a field location two landed records forbid without a ruling. Billy ruled each reshaping in session, and the third draft carried them: no predicate, no chosen location, and a stated demotion of how J0 is served today. The third review round found that the derivation had stopped at the node positions: the relation position's contents were transferred from a render record rather than derived, and the transferred edge `id` fails the criterion the draft had just generalised; the line's redefinition collided with `0096`'s worked example; a dangling-ref detection route used a form the draft parks; and the dispatch route offered for size cannot carry the between-members answer `0037` rules is the only one available. The fourth draft repairs each without moving a ruling. The derivation lands on `0038`'s seven fields plus the handle and the discriminator, with `state` re-typed as a position, which is an independent confirmation of `0038`'s selection on a different ground and not a transfer from it. The pre-registration, the reviews and the earlier drafts are kept as the exercise's raw artifacts.

### 0. The scoping point - ruled

**This ticket decides which reads a plan's inputs require. The plan's representation stays at #14 under its own gate.** #58's *Out of scope* line on the plan gains one qualifying clause and nothing else moves: *its representation; which reads its inputs require was decided at #86*. `0070` bounds this map at capabilities and not shapes. #14's evidence comment - three buckets replicating 6 of 6, a dated sequence, items owed with no date, items needing Billy - bounds what a read must supply without saying what the plan is; it is evidence, not a ruling, and §3 uses it as a bound. That figure is a fall26 number and carries the standing caveat.

### 1. The test's formalisation - deliverable 1

`0101`'s test in its informal wording: *a form belongs in routing's domain if and only if some judgment the coordinator must make ranges over a set that cannot be produced in one return without it.* The first application (#85's resolution, its §7) exposed three questions that change verdicts. Each is answered here, and a fourth the reviews exposed is answered with them.

**(iii) A form is counted at the grain where removing it changes what a chain can produce.** This is the test's own criterion applied to its own unit: `0101` says the test *"admits by what a form lets a chain produce"*. Under it, `FILTER(P and Q)` is a spelling of `FILTER(P) · FILTER(Q)`, so `and` is not a form. `or` is a form: without it, `FILTER(P or Q)` is `START · FILTER(P) ∪ START · FILTER(Q)`, a union, which the vocabulary does not contain and which is two returns. `not` and `exists` are each a form: `exists` is what carries a join, and under (i) below `not exists` is what tests absence on one member, which `0003`'s set-difference query is the ruled instance of. #85 §7's *connectives as one form* is replaced.

**(ii) Two chains produce the same set relative to a judgment.** Two chains produce the same set for a judgment if and only if they differ only at positions the judgment does not range over. Read absolutely over the path set, every distinct chain trivially produces a distinct set and question (i) loses its discriminating power; read absolutely over endpoints, `0101`'s ground for paths - *"a judgment across obligations is a judgment about their relations"* - is discarded. The relative reading keeps `0101`'s output type unchanged and makes each must-judgment declare what it ranges over, which is a column of §3's table.

**(i) *Without it* is read over the candidate forms minus the one under test, including inside `exists`.** #85 §7's provisional reading is confirmed. The fixed point the body feared - that the set of forms is what the test generates - dissolves under (iii): two spellings of one production are one form, so a candidate can never be excluded by a spelling of itself. The concrete case #85 §7 named, #88's *covered* disposition admitting `nodes_without` as a form and thereby removing the ground under `exists`, does not arise: `nodes_without(K, L, D)` is `START(K) · FILTER(not exists(MOVE(L, D)))`, a spelling, and whether it is separately named at the surface is #88's and says nothing to the test.

**(iv) A superset that carries the discriminator does not produce the set.** A judgment's set is the set as the judgment defines it. If a chain that returns a wider set with the deciding field visible on every member counted as producing the judgment's set, then no `FILTER` would ever be necessary at a width the coordinator can hold, and #85's first application admitted `FILTER` on exactly the ground that *no crossing reaches a date*. So the test counts the set, not the affordability of a superset; affordability is `0101`'s separate width question. Re-run over #85 §7's table, (iv) changes no verdict and strengthens the field-comparison row's ground. It is what §8 applies to a filter on a link's field.

**The test can now be run.** Its instrument standing is unchanged: a finding at #87 or #88 that it admits or excludes wrongly revises it.

### 2. The type questions - ruled here

**A crossing may be optional.** A member whose crossing finds no relation is kept with an empty position, and the empty position names the relation it ranged over. Ground: *what each obligation requires you to know* is in `CONTEXT.md`'s opening sentence, so it is a must-judgment; a mandatory crossing returns only the obligations that require something, and the reader cannot tell *requires nothing recorded* from *out of range*, which is the silence `0081` forbids and the omission axis of **faithfulness** names; the complement fetched separately is two returns, which `0101`'s premise forbids. **A constraint on the grammar found here:** optionality scopes over the crossing together with any predicate on its target or on the link. `MOVE(about, pointed-by)` reaches both annotation kinds by `0012`'s signature (`annotation → any`); if a kind predicate is applied after an optional crossing, an obligation with a `sticky_note` and no `progress` record is kept by the crossing and dropped by the predicate, and the optionality has done nothing. The spelling is the grammar's; the type requirement is ruled: *an optional step is empty when nothing satisfies the step's whole selection*, written below as `MOVE(L, D)[selection]?`. A path may therefore end at an empty position; every non-empty node position is addressable, and which one the coordinator spends its next call on is its judgment. What a step from an empty position does is the grammar's.

**Every node position carries its kind's line, and the criterion runs over every position.** `0101` states its criterion on *"a routing result's endpoint"*. `0039`'s symmetry is scoped to the set the judgment ranges over, and a judgment over paths ranges over every position (§1 (ii)), so the criterion applies at every node position and not only the last: a position carrying only an id would make *which concept* in job 2 cost one `look_at` per intermediate, which `0101`'s premise forbids; a position carrying a block would carry depth the discard (`0043`) and `0095` bar. `0101`'s wording is amended from *endpoint* to *position* (§10). A position's `kind` is fixed by the step's selection where the step names one, and `0097`'s rule that a member omits what its container fixes applies. Course's line is `id` `name` `term` (`CONTEXT.md`, *the line*); `concept`'s and `artifact`'s are the debt that entry records against #17, #19 and #20, and this ruling fixes the type and not their content; `progress`'s is derived at §5, since it is a position of the standing intent.

**A relation position carries the link's fields that pass the same criterion, and today that is `role` alone.** A link's fields are `kind`, `role` where the kind has one, and `locator` (`0017`; `0012` gives `role` to `spec`), plus the `id` `0096` gave the edge so that `attach` and `detach` can name it. Run the criterion over them. `role` passes where a judgment turns on it: J7 cannot decide which `spec` artifact is worth the call without it (§3). `locator` fails: `0082`'s ground, that it *"says where you land after walking, which does not bear on whether to walk"*, holds on a path too. The link's `kind` and its direction fail wherever the intent fixes them, which every chain written here does, and `0097`'s rule that a member omits what its container fixes applies. The edge `id` fails both clauses today: no admitted form starts from or references an edge id, and `0101` leaves open whether an edge's `id` is an address at all and what `look_at` of an edge would return; `0096`'s ground for the `id` is a write's need to name the link, which is a repair concern the test does not reach (`0101`). It enters a relation position on the day an edge id becomes an address. This is the path's relation position and not the `<edge>` element, which `0096` and `0093` render with `id`, `type` and `direction` for a block's own reasons; those records are untouched. The first draft ruled what a node position carries and was silent on relation positions; §8's verdict on a link filter turns on this.

### 3. Which judgments the coordinator must be able to make - deliverable 2

**Sources.** A judgment is *must* if it is derivable from a ruled source: `CONTEXT.md`'s opening sentence, or `0001` as rewritten at #85 - its second job, its third job, and its sentence that *"helping model an assignment's requirements is in scope"*. #14's three buckets are evidence and bound the list (§0). Billy's raw decomposition in #84's body and #85's S1-S7 are candidates: each is either derived from a source below or parked with a wake (`0007`). This is #84's own acceptance standard - *expose every necessary operation, never a workflow per imagined scenario* - applied to the list.

| | judgment | source | ranges over | served by |
|---|---|---|---|---|
| J0 | what to do next, across five courses | Billy, in session (§6); #14's buckets bound its inputs | J1's set, plus two per-member observations (§6) | its set by the standing intent; its observations by asking until their record exists (§6) |
| J1 | what is owed, across five courses, dated or not | opening sentence | endpoints | the standing intent (§4) |
| J2 | which concepts each obligation requires | opening sentence | positions: which obligation, which concept | `MOVE(requires, points-at)?`; no concept node exists yet (#25). **Whether one hop or the `requires` closure** - `0012` gives `requires` a `concept → concept` signature too - is #88's closure question, and J2 is its candidate judgment |
| J3 | where the material that teaches those concepts lives | opening sentence; `covers` is the teaching relation by `CONTEXT.md`'s entry | positions, two crossings | `· MOVE(covers, pointed-by)?`; the artifact layer is #20's; inherits J2's hop question |
| J4 | which obligations across courses require the same concept, and which | `0001` job 2, via `0100`'s ground | positions compared: `course ≠ start.course` | the position comparison |
| J5 | *which items need Billy* - **a candidate, not a must**; the number is kept so the reviews stay readable | #14 bucket 3, which is evidence | - | parked, below |
| J6 | which concepts no artifact covers | `0001` job 3, via `0003` | endpoints | `START(concept) · FILTER(not exists(MOVE(covers, pointed-by)))` |
| J7 | which artifact states this obligation's requirements | `0001`, the requirements sentence | a relation position: `spec` with `role = given` | `MOVE(spec, points-at)[role = given]?` (§2, §8). That `given` names the artifact stating the requirements and `owed` the deliverable is this record's reading of `0012`'s enum; no landed record glosses either value, and #85 §7's gap table is the only prior |
| J8 | which obligations this one builds on, and which build on it, across courses | `0001` job 2; `builds-on` is the one cross-obligation relation that exists before the concept layer (`0012`, `0092`) | positions: both obligations and the direction | `MOVE(builds-on, D)?` in either direction; no new form |

**Bucket 1 and bucket 2 are J1 with `0041`'s order.** A dated sequence is J1's set ordered by `due`; items owed with no date are J1's members with a null `due`, which `0041` places last and which the standing intent may therefore not exclude. `0042` says the same of an undated obligation: *"it is present, it is routable"*.

**Bucket 3 - *which items need Billy* - is a candidate, ruled so in session.** Its only source is evidence, and its predicate has three structural readings no record picks between: a conflict the agent may not close itself (`0051`), a question asked and not yet answered (#10's subject; `0056` governs an answer already kept), a progress claim surfaced and unconfirmed (`0010`). Under `0101` an intent the system cannot yet serve is refused, never returned empty. Parked; wake: the first real instance of an item needing him, observed by hand (#16's wake), read for what *needing him* meant.

**Candidates not on the list, and where each went.** *Lecture progress - where the course has got to* (Billy's raw decomposition): not derivable from a ruled source; that no structure carries it (`0021` keeps the time axis and a course's coarse grouping out of the skeleton) is why it cannot be served today, not why it is not a must. Parked, wake: the first real decision that turns on it. *Absorption* is §6's. *Material reached by another route than `covers`*: `0012` has two further link kinds between an obligation and an artifact - `spec`, whose `given` role J7 uses, and `prepares-for` - and no ruled source names a judgment over `spec`'s `owed` role or over `prepares-for`, or over any union of routes with J3's; each would be a chain of its own, so no judgment needs `or` over crossings. Parked with J7's wake. S1 as stated is #87's whole; S2 is caller-agnostic and its two readings are J8's chain and J4's; S3 is §4; S4 is J6; S5 is J1 · J2 · J6 composed and needs no new form; S6 is J4; S7 is §8's.

### 4. What this makes ring 0 - the standing intent

**Ring 0 is one routing result held resident by policy (`0101`), and this is the result.** The standing intent is derived from what J0 and J1 range over, under `0101`'s clause that an unprompted coordinator needs somewhere to stand.

```
START(obligation) · MOVE(about, pointed-by)[kind = progress]?
```

A path of two node positions and one relation position: **obligation - `about` - progress**, the crossing optional under §2's rule. Every obligation is a start; its current progress record is reached by crossing `about` backwards, and `0035`'s *"one current value per target"* is read here as one record per target, edited in place, which `0028`'s per-field update and `0036`'s in-place modification support. An obligation with no progress record keeps its path with the progress position empty, which `0035` reads as `not_started` and `0082` renders without an `id`. `course` is a Ref-typed field on the row, a bare pointer answering *which one* (`0082`), whose `id` is the supplied course code (`0026`) and is readable as one.

**Why it starts from `obligation`, and what that costs - ruled by Billy.** The first draft started from `course` and reached the obligation by crossing its `course` field backwards, so that a course with no obligation would keep a path and speak for itself under `0081`. Review showed the cost: `0029` makes `obligation.course` mandatory, but `0018` lets a ref dangle and rules that deleting a course does not cascade to its obligations, and `0091` records that *"a coordinator can therefore name a course row it never read"*, so an obligation whose `course` names no node is produced three ways - landed before its course, orphaned by a deletion, or carrying a code no course has. Every such obligation is owed, and a range over `course` cannot reach it; nor can a union add it back, since set operations are outside the vocabulary (`0101`) and a second chain is a second return. The two shapes each have one blind spot and they are not alike. Starting from `course` drops an owed obligation from the resident set silently, which is the omission axis. Starting from `obligation` keeps it, with a `course` ref that names nothing; ring 0 does not detect that, and the coordinator's next call from the row - `look_at(obligation)` - does not either, since a Ref-typed field renders as a bare pointer and `0097` makes it addressable rather than existent. Detection is a repair read, which `0101` puts outside the test's reach: `0018`'s link-set validation pass, *"owed and unbuilt"* by its own words and extended to Ref-typed fields by §10. An absence query over the `course` field would find it in one return, but that is a crossing of a Ref-typed field, which §8 parks, so it is not offered here. A course with no obligation is not a judgment any ruled source names; the course lines are one on-demand `START(course)` away. **Ruled: the intent starts from `obligation`; an owed member kept with a bad pointer beats an owed member dropped; the dangling ref is a defect the design tolerates by `0018` and a repair read finds; the linear path type is why obligation cannot both be the start and sit between `course` and `progress`, and a multi-source start is a union.**

**Why there is no predicate, and what that assumes.** J0 and J1 range over every obligation, dated or not. No ruled source names a judgment that ranges over *the active ones* as a set: the opening sentence says *what is owed*, bucket 1 says *a dated sequence*, neither says *within a window*. Which obligations are near is a judgment the coordinator makes with the set, using `due`, `done_by` and `state` against today, and `0101` places a judgment with operations rather than in them. §7 says what this does to `0042`. **One term is assumed, and the assumption is part of the width bet below.** The opening sentence says five *concurrent* courses; no record confines the skeleton to a term - `0037` row 11 defers `course.offering_term` to v2 on the ground that v1's boundary is coursework - so once a second term's courses land a bare `START(obligation)` ranges over both. The second draft carried a term predicate as a chain over `course`, and review showed it dropped the dangling-ref obligation the paragraph above keeps; and the value it compared against, the current term, is held nowhere - `0037` rows 13 and 14 leave term boundaries open. So the predicate is not written. Wake: a second term's courses land; what wakes then is the predicate and its held value, and it is written so that an obligation whose term cannot be determined stays in.

**Why it reaches `progress`.** `state` is J0's input - in-progress work is one of the three triggers Billy stated - and it is a field of `progress`, not of `obligation`, by `0035`, which is also the record that bars copying it onto the obligation: *"`state` sits on `progress` and not as a field of `obligation`"*. #71 holds the question of whether `state` moves onto `obligation` open; if it ever does, the progress position is re-typed, and §11 lists it. `about` is in `0012`'s table and crossable. **`0046` is a delivery rule and this is not the read it governs.** `0046` says a read that returns a node's neighbourhood must deliver annotations through their own channel and never as ordinary neighbours; `reading-records.md` records that reading it as a scope rule is a misreading. A resolve's path is not a node's neighbourhood: the progress position carries `progress`'s line (§5) and not its content, the content still arrives only through `look_at`'s channel, and the path shows the crossing that reached the position, as `CONTEXT.md`'s `applies` entry already says of a resolve. `0046` gains one sentence saying so (§10).

**What this changes in `0100`'s clause table.** `0100` placed `0042` under **admission** - *which rows enter* - and separately noted that what `refresh()` returns is band B's field set, *"which is the selection clause"*. `0042` never excluded a row: every obligation enters, and the partition decides which rows carry three more fields. That is a per-row field-set partition, and `0100` had split it across two clauses rather than placing it in one. `0100`'s own doubt - *"`0042` does not itself say that ring 0 ranges over every obligation in a semester"* - was about the range, which this record settles from the judgments and Billy's account (§7), and with which `0042`'s *"it is present, it is routable"* agrees for the undated case. Admission is the standing intent's range, ruled above; `0042`'s content is §7's.

**Width.** Five courses is roughly 55 obligations, a fall26 number whose evidence base excludes the obligation-dense course (`0038`'s scope clause); the standing result is one return of that width for one term. #80 measured ring 0 at that row count as *"flat 6,482 characters, structured 7,598"* (#82's body, which carries the scope clause that band A is 21% of rows in one course and 2px3 would move it); under this record every row grows by what band B lacked - `done_by`, `optional`, `has-more` - and by the progress position's `origin` and `updated_at`, so that figure is a floor and not the bet's number. `0101` leaves the width gate unruled, and this record does not rule it: holding every obligation resident is a bet on that figure and on one term, recorded as one. Wake: the first measurement over a fixture that includes the dense course, or a second term landing. If it breaks the bet, what wakes is the width gate or the term predicate, not a window on the intent.

**The time projection is not resident.** `0021`'s query-by-time-period is a routing result (`0101`), and under this record it is on demand, expressible today as `START(obligation) · FILTER(due ∈ range)` with the range the coordinator's. Nothing in §3 ranges over it as a standing set, and the coordinator already holds every `due`. What the projection holds beyond that - `time_point`, location, duration - is #13's.

**What `refresh()` reports.** `0089` returns a whole line per changed obligation; under this record the unit is the row (§5), and a change at the progress position - a record created or its state changed - is a change to the row it hangs from. `0089`'s ground and its return shape are untouched; its noun and one pointer are owed (§10).

### 5. What a row carries - derived at both positions

`0101`'s criterion, as §2 generalises it: *a field belongs on a routing result's position if and only if, without it, the coordinator cannot form its next intent or cannot decide whether to spend the next call.* Applied field by field, for J0 and J1, which are the judgments the resident result serves. The field lists were assembled from this repository's records - `obligation`'s from `0027` (`kind`), `0028` (`added_at`), `0029` (`course`), `0032`, `0033`, `0037`, `0038` (`name`, `due`, `optional`, `done_by`) and `CONTEXT.md`'s `name` and `due` entries, and `progress`'s from `0028`, `0035`, `0036` and `CONTEXT.md`'s `origin` entry - because no landed record carries a field table; `0082`'s Source line points at the fall26 schema for that, which is provenance only.

**The obligation position:**

| field | under the criterion | verdict |
|---|---|---|
| `id` | the address fetch accepts; `0082` makes it an attribute because it addresses | in, structurally |
| `kind` | the discriminator (`0027`); `0082` makes it the element name because it addresses | in, structurally |
| `name` | an id says nothing about the record it names (`CONTEXT.md`); without the name no call can be judged worth spending | in |
| `due` | J1's dated sequence does not exist without it | in |
| `done_by` | Billy's chosen target date, a direct J0 input | in |
| `optional` | a J0 input: whether the obligation is to be done at all. Whether an `optional = true` obligation is *owed* is a J1 membership question this record does not decide; it is included and the coordinator judges | in |
| `course` | a Ref-typed field, a bare pointer (`0082`); J0 is a judgment *across five courses* and cannot be made without knowing which course each member belongs to, and the pointer is what the next intent over a course is formed from | in, as a field |
| `has-more` | in no field table (`0082`'s second exception); `0101`'s spontaneity paragraph names it as the in-band carrier of which vocabulary applies here, and the criterion's first clause - forming the next intent - depends on it. `0092` rules its value - the set of link kinds present on the node, a set and not a count - and that ruling is untouched: on a row whose progress position is non-empty, `about` in the set says an annotation exists, which the position also shows for one kind, and whether a `sticky_note` exists besides is not something a set of kinds ever claimed. That a row then shows one fact twice is a render collapse, and **where the field appears and how the duplication collapses are #82's by name**, under `0097`'s rule | in, as the row's one non-field item |
| `parts` | J0 and J1 do not need *what this is about*; that is J2's, served by the `requires` crossing once concept nodes exist (#25). `0038`'s ground for excluding it holds on the new derivation | out; returns with any `look_at` |
| `grade_share`, `grade_share_conditional` | J0's inputs Billy named are size and absorption, not weight, and J1 does not turn on a share; that it has no reader by standing exemption is why nothing else admits it | out |
| `added_at` | `0099`'s ground - it is about the row, not the node - applies one render over; *no reader* is the ground `0099` declines, and it is not used here | out |

**The `about` relation position carries nothing today.** Under §2 a relation position carries the link's fields that pass the criterion: `about` has no `role`; its kind and direction are fixed by the intent; its `id` is not an address by any landed record. The position exists on the path and is empty of fields; how a render shows or elides it is #82's, under `0097`'s rule.

**The progress position:**

| field | under the criterion | verdict |
|---|---|---|
| `id` | addresses when the record exists; a defaulted `progress` carries none and that absence is the signal (`0082`) | in, structurally |
| `kind` | fixed by the step's selection; `0097` applies | in, structurally |
| `state` | J0's input; the trigger `0042` stated as `state == in_progress` | in |
| `detail` | the kind's one free-text field; a line carries no content (`0095`), and the content arrives through `look_at`'s channel | out |
| `origin` | the criterion's second clause: whether to spend a call re-confirming an `in_progress` depends on how it came to exist - said, or asked and confirmed (`0010`) - as much as on when. `0056` is why the harm is real: an asked answer's *"provenance is stated prominently at every read"*, and a resident `state` is read at every turn | in |
| `created_at` | when the record entered; no judgment changes on it | out |
| `updated_at` | `0099`'s harm - *"a January answer is indistinguishable from today's"* - lands on a resident `state` as directly as on a block, and whether to spend a call re-confirming a stale `in_progress` is the criterion's second clause | in |

**The row is `id · kind · name · due · done_by · optional · course · has-more` at the obligation position, and `id · kind · state · origin · updated_at` at the progress position.** The obligation position is `0038`'s seven with `state` re-typed as the progress position, plus the handle and the discriminator `0038` left implicit, on every row. The progress position is a new line for `progress`, one ruling per kind as `0095` requires; `CONTEXT.md`'s *the line* entry says `progress` needs none, which stays true of a neighbour in a block, where the channel delivers it whole, and is qualified for a path position (§10). The derivation runs from the judgments to the fields, which is the direction `0070` asks and `0100` found `0038` did not run; that it lands on the same seven is a confirmation of `0038`'s selection, and its band B - the reduced set - does not survive (§7).

**Row and line are two things, and only one of them carries `has-more`.** A ring 0 **row** is a position of the standing result and cannot list its neighbourhood, which is `0092`'s and `0096`'s ground for `has-more` living there. Obligation's **line** - what a neighbour renders as inside an `<edge>` - sits inside a `<neighbours>` section that already lists the edges, and `0082` as repaired at #80 and `0096` both rule `has-more` off it. A line is self-closing (`0097`) and has no positions, so it cannot be *the row minus `has-more`* literally; what transfers is the field set, which is what `0082`'s transfer clause always meant. So: **obligation's line is the obligation position's fields, `id · kind · name · due · done_by · optional · course`, plus `state` carried as an attribute**, which is how `0096`'s worked example already shows it - `state` is a J0 input on a neighbour exactly as on a row, and it is the one field that says whether a neighbour has been started. Against band B the line gains `done_by` and `optional`; `0096`'s example is repaired to carry them (§10). `0082`'s transfer clause, stated twice there, and `0095`'s *"the band is band B"* are repaired to say the same (§10).

### 6. Size and absorption - ruled, and split

**Two claims Billy made in the session, and the test touches only one.** First, that deciding what to do next across five courses is hard to do without an obligation's size and his own absorption of a concept. That is a must-judgment's declaration - J0 - and he is the only witness to it. Second, that he does not know how to model either into the system. That is a representation question, and it is not routing's.

**Neither size nor absorption is a form. Each is an observation on a member.** J0 ranges over J1's set; routing's obligation ends at delivering that set in one return. Size and absorption belong to `0039`'s layer - *observe anything you can afford for every member; else dispatch* - which `0101` keeps separate from the one-return premise in so many words: *"Affordability is a separate question the type does not answer … `0039`'s affordability is per-member depth."* The second draft turned the premise into a constraint on the observation's record, and review showed that was the premise read for an aspect it disclaims. **So the `0042` sentence #82 authorised this ticket to overturn - *the system holds no notion of an obligation's importance* - is not overturned.** It bars a rule that promotes rows by importance; J0 is Billy deciding, with the system supplying inputs. The permission is not used for it. What the permission does open is absorption's record: #84 records that `mastery` on `progress`'s *Avoid* list and `concept`'s retracted definition were deferred rather than overlooked and are unfrozen by it.

**How the two inputs enter, and what is parked.** Three records say how: `0033` rules that size's replacement is *an interaction*, not a field; `0010` rules absorption is owner-authored and an agent may surface a claim but never resolve one; `0056` rules an asked answer is kept, with loud provenance. Composed: both are things Billy is asked, answers, and the system remembers. **What record holds the answer is parked whole - its kind, its location and its type.** `0039`'s formalism is what will sort a candidate record: a `sticky_note`'s content is its one free-text field, which no line carries (`0095`), so as a `sticky_note` the observation is the dispatch branch, unaffordable for fifty-five members in one return; a typed field on a position of the standing result would be the observe branch, but `0033`'s *"not another field"* and `0037` row 1's no-re-add rule bind that choice, and a field is chosen only by a new ruling this record does not make. Two things the instance has to answer first: `0037` row 1 rules that the answers available are *"ordinal comparisons, not hour counts"*, a relation between two members that a per-member field does not hold; and whether what Billy says is a value or a sentence. Wake: the first real decision observed by hand (#16's wake) - the first time Billy tells the coordinator that an obligation is large or a concept is not yet absorbed, what he said and how it was used are recorded, and the record's shape is ruled from that (`0068`).

**Absorption's target is a concept, and that waits on a layer.** Absorption is of a concept, so its record hangs on a `concept` node - `0012`'s `about` signature admits any target. Owner-authored, which `0010` permits; not the system-inferred mastery `0010` bars; and `CONTEXT.md`'s `progress` entry, *"how far along its target's work is"*, does not yet cover a concept as a target, which is one more thing the wake decides. It is two crossings from an obligation (`requires`, then `about`) and behind a layer that does not exist (#25), so it is unreachable today whatever its shape.

**What serves J0 today - ruled, and it is a demotion, with one gap stated.** J0's set arrives in one return by the standing intent. Its two observations have no record yet. **Absorption** arrives by asking Billy, which `CONTEXT.md` calls one case of dispatch, under `0039`'s formalism: *how far along are you with concept X* is a per-member answer, which is the shape `dispatch(X, member)` returns. **Size does not fit that route, and this record says so rather than ruling it in.** `0037` row 1 rules that the only answer available is an ordinal comparison, *"not hour counts"* - a relation between two members - and `0039`'s dispatch returns a value of one member in the same shape as every other's, which has no position for a relation. So how size reaches J0 today is open, and the wake §6 names is where it is answered; the instance may show that size is a relation across the set rather than an observation on a member, which would put it nearer `0101`'s ground for paths than to `0039`'s layer, and that is noted for the wake and not ruled. This is not a failure of the necessity test, which admits forms and not observations. `0101` leaves *whether dispatch calls count against the effectiveness constraint* undecided; absorption's asking is that question's first concrete instance, and it stays open (§11). #82's refusal to widen ring 0 to supply the plan's inputs stands: nothing here puts an input on a row that the criterion did not admit.

**#14 is not contradicted.** #14 says *"if size is judged from progress and load rather than stored as a number, then the plan is where that judgment gets written down, or nowhere."* That sentence is about where the **judgment** lands, which is the plan's representation and #14's under §0. This record is about where the **input** lands, and parks it. The two are different objects.

**A conflict between two landed records, found here and to be repaired.** `0037` row 1 says size is *"observed rather than stored - ordinally, from `parts` and item notes first, then by asking for a relative comparison"*. `0033` says `parts` *"does not carry size"* and `CONTEXT.md`'s `parts` entry bars *"using it to judge how much work something is"*. Both cannot stand. Under this record size enters by asking, so `0037` row 1 is repaired to drop `parts` as a first source; its ruling on ordinal answers is untouched.

### 7. What `0042` was, and what it becomes - ruled

**`0042` was a per-row field-set partition, not an admission clause.** Billy's account in the session: when `0042` was ruled, ring 0 was assumed to hold every obligation, and the partition expressed that not every concern deserves attention - only the active ones needed the three further fields, the rest needed only to be known to exist. Every row entered; the partition chose what a row carried. `0100`'s placement is corrected (§4).

**Under `0101` a result carries one field set per kind, and ring 0 carries one.** `0101`: *"Each endpoint carries its own kind's deciding fields - one set per kind"*, endpoint read as position under §2's amendment, and depth about any member is fetch's. The concern `0042` served does not disappear; it changes hands. The coordinator holds a symmetric set, judges which members are near, spends `look_at` on those, and drops what it fetched (`0043`). **Ruled by Billy in the session:** the rows carry one field set. `0042`'s defence - that two bands do not violate uniform depth because the partition is computed from material facts plus one rule applied identically - was argued against `0039` and is not refuted here; what the partition fails is `0101`'s output type, which is stricter than `0039`. The practical ground Billy accepted: three fields on the rows the partition would have thinned, plus the progress position's two, cost little at this width (§4 carries the measured floor), and a ring 0 whose shape does not depend on today's date is a purer routing result.

**What survives of `0042`:** the three triggers, as the coordinator's judgment rule for *near*; the sentence barring a notion of importance (§6); the ruling that breadth is never a defect. **What dissolves:** the partition as a property of ring 0, the band names, and band B's reduced field set. **What the coordinator gains:** the three triggers are now its own rule, so it can widen the date window in an exam week without the system changing.

### 8. The two forms the body asked about, and the parked ones - deliverable 4

**Crossing a Ref-typed field from a computed set has no must-judgment today.** The first two drafts had J1 cross `course`, and each crossing was what lost the dangling-ref obligation; the third does not cross it. J4 compares the `course` value at two positions, which is a comparison and not a crossing; J0 across five courses reads the pointer, which `0026` makes the course code. `0084` composes the backward crossing into `look_at(course)`, which `0101` calls resolve entering through fetch and which is untouched. S7 - *the obligations of the courses Billy annotated "dropped"* - is the shape that would need it and is agent-constructed. `0101` places the crossing in the vocabulary; `0102`'s membership row leaves its admission to this ticket, and it stays open. Parked; wake: a must-judgment from a ruled source that needs a course's fields on the path or starts from a computed set of courses.

**Filtering on a link's own fields is a spelling of `FILTER` at a relation position, and its must-judgment is J7.** Once a relation position carries `role` (§2), `MOVE(spec, points-at)[role = given]?` is a selection scoped over the step as §2 requires, and a predicate on a relation position's field is the same production as a predicate on a node position's field, not a separate form. J7 - *which artifact states this obligation's requirements* - ranges over the `given` links and not the `owed` ones, and under §1 (iv) the superset with `role` visible does not produce that set. So a selection over a relation position is admitted, by J7, and what #85's first application called `MOVE` with `Q` is one of its spellings. The one other link field, `locator`, bears on where you land and not on the set (§2), and no judgment names it.

**`or` has no must-judgment after §7.** Its one candidate was `0042`'s disjunction as a standing intent, and that intent is not standing. The routes from an obligation to material are separate judgments and not one judgment over their union (§3); the time projection's range is a single interval on `due`. Parked; wake: a must-judgment from a ruled source that ranges over a union.

**`REPEAT`** is #88's, with J2 as its candidate judgment (§3); the cost statements it needs are in `0101`.

### 9. Findings that bind the sibling tickets

- **#82 (ring 0's render) - unblocked.** The clauses it renders are derived: the standing intent (§4) and the row (§5). **Arrangement is assigned to #82**, ruled by Billy, which widens its scope from the render alone to the arrangement clause `0100` keeps apart from it; #82's body is to say so. Three inputs travel with it: `0041`'s order stands - `due` ascending, nulls last, among nulls by `done_by`, ties by the handle, never array order; no grouping default is set here; `0041`'s struck grouping and `CONTEXT.md`'s `reload` entry are to be read together there, over rendered data (`0040`'s method). Of the two orphans it carries, `has-more`'s name and position stay its own, and it gains the collapse §5 names, a row showing `about` twice; **the band's representation is dissolved** (§7) and that orphan closes. It also decides how a render shows or elides the empty `about` relation position.
- **#88 (coverage).** The forms admitted by this record: `START(K)`, `MOVE(L, D)` optional or not, a selection at a node or a relation position, the position comparison, `not`, `exists`. `nodes_without` is a spelling of `START · FILTER(not exists(MOVE))` under §1 and enters as J6's chain; whether it is separately named at the surface is #88's own question and the test does not decide it. J2 is the candidate judgment for the closure question; `closure` and `REPEAT` are otherwise untouched here.
- **#87 (ownership).** Untouched. J3's second crossing reaches an artifact node, not the store; S1 as stated remains #87's whole.
- **#79 (tool descriptions).** Still blocked on #87 and #88; the read side's operations are the grammar's.
- **#16.** Its wake gains three readings: what shape the size and absorption answers took when Billy gave them (§6), what *needing Billy* turned out to mean (§3), and whether the first real decision turned on lecture progress (§3).
- **#14.** Unchanged under §0; one comment noting §6's distinction between the input and the judgment.
- **#25.** One comment: absorption's record targets a `concept` node (§6), for when the kind acquires a writer.
- **#13.** One comment: under `0101` the time projection is a routing result and this record rules it not resident; what it holds is still #13's.
- **#71.** One comment: the progress position rests on `0035`'s placement of `state`; #71's answer re-types it.
- **#58.** The plan's *Out of scope* line gains §0's clause.

### 10. Proposed amendments - ruled by Billy's posting of this comment

1. **One new record** - *Ring 0's standing intent is a path from every obligation to its progress, and its rows carry one field set* - holding §1, §2, §4, §5 and §7: the formalisation, the type rulings, the intent, the rows' derivation, the term assumption, and `0042`'s re-placement.
2. **`0101`, four sentences.** *Endpoint* becomes *position* in the three sentences that use it in §2's sense - the path's endpoint carrying what decides the next call, each endpoint carrying its kind's deciding fields, and the criterion itself; a pointer to the new record for the formalisation and the type questions.
3. **`0042`, repaired in place.** Title and body restated as §7: the three triggers as the coordinator's judgment rule; the partition and band B struck; the importance sentence and the breadth sentence kept; the overturn paragraph replaced by a pointer.
4. **`0038`, repaired in place.** Band B struck; the field set restated as the row with `state` as the progress position; the ground replaced by the derivation; the exclusions' arguments kept as arguments; the sizing paragraph kept with its scope clause and the width bet named.
5. **`0100`, four places.** `0042` relabelled from admission to a per-row field-set partition; the admission row and the selection row of the clause table point at the new record; the refresh paragraph's *"a line, whose field set is band B"* becomes *a row, whose field set is derived at the new record*; the record's *not settled* standing updated to *derived at #86*. Its paragraph refusing to widen ring 0 stands as written.
6. **`0037` row 1, one clause.** `parts` dropped as a first source of ordinal size, on `0033`; asking kept.
7. **`0082`, two sentences; `0095`, two sentences; `0096`, one example.** `0082`'s transfer clause, in both places it appears, becomes *obligation's line is the obligation position's fields plus `state`*, with the derivation behind it. `0095`'s *"so the band is band B"* is replaced by the same, and its *no rule generates a new kind's line* gains `progress`'s as the first line derived under `0101`'s criterion. `0096`'s worked example gains `done_by` and `optional`.
8. **`docs/adr/README.md`.** The new record's row; `0038`'s, `0042`'s and `0100`'s rows restated to match their repaired titles.
9. **`0089`, one sentence.** Its return is a row, and a change at the progress position is a change to the row.
10. **`0046`, one sentence.** A resolve's path is not the read this record governs; an annotation reached by crossing `about` is a position carrying its line, and its content still arrives only through the channel.
11. **`0019`, one clause.** *"residency is an access policy over obligation nodes' fields"* becomes *over the fields the standing intent's positions carry*; the two-persisted-things ruling is untouched.
12. **`0018`, one clause.** The owed validation pass is named as reaching Ref-typed fields, which its own cascade row already implies.
13. **`CONTEXT.md`** - via the domain-modeling skill, not by hand: `ring 0` restated as the standing result with its positions, its *derivation owed* note replaced; `band A` / `band B` retired to an *Avoid* under `ring 0`; `obligation`'s *"the same nodes ring 0 is a projection of"* qualified; `path` gains the empty position and the relation position; `the line`'s *"obligation's is ring 0's band B plus the edge's `type`"* rewritten to the field set §5 derives, and the entry gains `progress`'s line as a path position and the row-versus-line distinction, keeping *a ring 0 row is not a line*; `parts` and `progress` unchanged, the latter pending the wake §6 names.
14. **Pointer debts, listed rather than fixed**, per #82's precedent: `0091` (*"Ring 0 carries the band"*), `0092` (its opening sentence, its Considered Options, and its Source line), `0093` (its ring 0 section, two rows of its rejected-names table, and its names row saying an edge carries `id`, `type` and `direction`, which is true of the `<edge>` element and not of a path's relation position), `0094`'s closing sentence, and `0013` (its cost argument's roughly-55 figure now rests on the standing intent's range rather than on `0042`'s admission clause) each describe the state this record amends and none contradicts it. Of the six #82 listed, `0044` reads clean and `CONTEXT.md`'s `the line` is item 12.
15. **Issue comments** as §9 lists them, and #84's Decisions-so-far gains this ticket's line.

### 11. What this ticket does not decide

The grammar's spelling (`0070`, `0093`, #84 *Destination*), including how an optional step, a selection at a node or a relation position, and a step from an empty position are written; the width gate, on which no record speaks, and the bet §4 records against it; the term predicate and where a current-term value would be held (`0037` rows 13 and 14), parked on a second term landing; whether a Ref-typed field's crossing is admitted (§8, parked); J2's hop count (#88); ring 0's arrangement and render (#82), including how the `about` relation position collapses; `has-more`'s name, its position, and how its duplication with the progress position collapses (#82; its value is `0092`'s and unchanged); whether an edge's `id` is an address, and what a relation position carries once it is (`0101`'s open question); *which items need Billy* as a judgment and its predicate (parked, #16's wake); the record that holds a size or absorption answer - its kind, location and type - and whether a `progress` may target a `concept` (parked, #16's wake; a field would need a ruling against `0033` and `0037` row 1); how size reaches J0 before that record exists, since the ordinal answer `0037` allows is not a per-member value (§6); *lecture progress* as a judgment input (parked); what the time projection holds (#13); `closure`, `REPEAT` and their width (#88); by-handle, a chunk's handle as an address, and who serves an intent by content (#87); the deciding fields of a `concept` or `artifact` position (#17, #19, #20); whether dispatch calls count against the effectiveness constraint, now with §6's instance; the repair reads `0101` rules the test does not reach, including the pass that finds a dangling `course` ref, with `0071`'s warning that a success-path derivation misses them carried forward; a multi-source start, which is a union and outside the vocabulary; whether `state` moves onto `obligation` (#71); whether an `optional = true` obligation is owed; `origin`'s write rule (`0056`); the plan's representation (#14).

**Standing of the numbers.** The roughly-55 figure and the 6-of-6 bucket replication are fall26 numbers and are not auditable from this checkout.
