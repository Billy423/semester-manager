# Review 6 - draft v5, lens 2 (falsification)

**Reviewer:** a fresh blind subagent (Claude Sonnet), given `drafts/v5.md` and full repository read access. Withheld: the session transcript, the pre-registration, reviews 1-5. Brief as review 5, with one change to the thresholds: §7 is declared a draft, so a finding that §7's list is incomplete or a §7 verdict is wrong is repair; §1-§6 and §8-§10 are ruled and attacked at full severity.

**Report, verbatim:**

---

## Findings

1. **Attack 1 - the ruled predicate type cannot serve `0001`'s second job in one return, and §10 hands the repair to a ticket that cannot change §2.** §2 rules: *"A predicate ranges over the vocabulary - a member's fields, and the relations it participates in - and may itself contain a chain used as a test on one member (the join)."* A predicate over another position of the same path (`course ≠ start.course`) is outside that type: a join's inner chain starts from the member and has no reference back to the outer one. The judgment is `0001`'s *"manage cross-course information in the background"*, which §3 itself reads via `0100` as *"a statement about relations across obligations"* and makes the sole ground for paths. §7 concedes the set is not produced (*"the cross-course set arrives one course per call"*), and §10 defers *"how a predicate compares two path positions (#86)"*. But #86 decides judgments; changing what a predicate ranges over changes the type §2 rules. Either the type as ruled cannot serve job 2 exactly, or (if a superset return counts) §7's gap statement is false and finding 4 bites instead. Both branches change a ruled section. `blocking`

2. **Attack 4 - §3 and §9.3 gate a resolve's width on a bound that governs text length, and §9.3 writes that into `0060` and `0004`.** §3: *"what happens then - a narrower intent, a bound, or dispatch of the whole - is the owed length bound `0039` names as load-bearing (#12 item 6)"*; §9.3: *"its gate is the owed length bound `0039` names (#12 item 6), not the expansion cost of one block"*; §10 repeats it. #12 item 6 is *"The length bound on free text entering the resident skeleton, over two routes: `sticky_note.body` and the ingest-written summary."* `0039`'s affordability is per-member depth - *"eight one-line summaries can be pulled for a comparison set, eight paragraphs cannot"* - not the count of members. A resolve over a whole kind or a cyclic repeat is wide by member count; no landed record names a bound on that quantity. A cited record does not support a claim that §9.3 makes a ruling and amends two records with. `blocking`

3. **Attack 1 - the ruled output type is not finite for a ruled step over a landed relation.** §3 rules *"Routing returns a set of paths"* and *"Deduplication is by path"*; §2 rules a step may *"repeat a crossing"*. `0012` rules only `part-of` a DAG; `requires` (concept → concept) and `builds-on` (obligation → obligation) are not ruled acyclic. `REPEAT(MOVE(requires))` from a concept on a `requires` cycle yields unboundedly many distinct paths, so the ruled output is not a set the type can return. The draft knows this (§7 gap 2: *"has unboundedly many paths"*) and defers the fix to #88 as *"how a repeat over a cyclic relation is bounded"*. Every fix - define path as simple, bound the repeat, or read the result as endpoints (which §3's *"Paths, not endpoints"* excludes) - changes §3's ruling, not §7's list. `CONTEXT.md`'s `closure` is *"the set of nodes reachable"*, finite; the draft's own §7 says paths *"are wider than that"*. `blocking`

4. **Attack 2 - §4's premise is not entailed by its stated ground, and the difference is what the test turns on.** §4: *"the set must arrive in one return. The ground is Billy's effectiveness constraint... the coordinator's call count may not grow with the graph. A set assembled across calls costs one call per piece."* One call per piece grows with the graph only when the number of pieces does. A union of k fixed chains is k calls for any graph size; nine one-per-kind chains (§7's wildcard row: *"nine chains, one per kind, and their union is not a chain"*) is a schema-size count, not a graph-size count. So *"no finite chain of the other steps and forms produces in one return"* is stricter than the ruling it rests on, and a sibling ticket applying the boxed test admits forms the constraint does not require (S2's two routes, `builds-on` and shared-concept, are exactly a fixed union the type cannot express since *"Set operations are not composition"*). Para 2 records only the constraint as Billy's ruling; the one-return premise is the draft's step from it. Weakening the premise to match its ground changes the test. `blocking`

5. **Attack 2 - the test's key relation, *produces*, is exact-set or superset, and the draft never says which.** §4's box: *"ranges over a set that no finite chain of the other steps and forms produces in one return"*. Under a superset reading `START(K)` produces every set within a kind and no `FILTER` or join is ever admitted - which fails #84's own check (*"the routing test must generate [`nodes_without`]"*). The draft's usage is exact-set (§7 `Q` row: *"the crossing without `Q` returns a superset"* → in), but §7 gap 1 then contradicts §3: `START(obligation) · MOVE(requires) · MOVE(requires, pointed-by)` returns the whole cross-course superset as paths in one call, not *"one course per call"*. Inserting *exactly* into the box changes no intended ruling. `repair`

6. **Attack 4 - §6 and §10 assign the second-holder existence question to #66, which #66 and #84 both refuse.** §6: *"Whether a second holder exists is #66's question as #84 records"*; §10: *"whether a second holder exists (#66)"*. #84's fog: *"It is upstream of #66 rather than parked there. #66 asks a narrower question"*. #66's Billy comment (2026-09-05): *"It does not change the question - which is still `0081`'s residue... not whether such a layer should exist."* The attribution is the opposite of both. §9.6 keeps the patch in #84's fog, so no decision moves. `repair`

7. **Attack 4 - §2 makes an edge's `id` a fetch address and thereby amends `0089` without listing it in §9.** §1: *"Fetch takes an address"*; §2: *"An address is... a node's `id`, and an edge's `id`"*. `0089`: *"`look_at(id)` reads one node."* The draft notes `0089` *"predates `0096`"* and stops; `0097` supports the reading, but `0089`'s clause is altered and `0089` appears in §9 only as #88's. `repair`

8. **Attack 4 - §2 turns direction into a value the coordinator names back and does not carry the consequence to `0098`.** §2 rules a crossing is *"a link kind in a direction"*; `0098`'s split is *"whether the coordinator can name the thing back"*, and it lists `points-at / pointed-by` under *"invented here → free"* because nothing named them back. The draft cites the classification and hands the spelling to #79, but its own ruling moves the values into `0098`'s first class, where the schema has no spelling to point at. `0098` is not in §9. `repair`

9. **Attack 3 - §6's *"no clause's present content is cited here"* is false in its own paragraph.** Two sentences later: *"`0089` rules what `refresh()` returns - whole lines per changed obligation"* - that is the refresh clause's present content (`0100`'s table row). Nothing is derived from it, so the derivation direction holds; the sentence does not. `repair`

10. **Attack 4 - §9.1's restatement of job 1 removes its routing derivation while §7 derives chaining from job-1 scenarios.** §9.1: *"remove the anxiety of not finding information is reachability's felt form"*; §1: *"Mechanistic reachability - that a path exists in the skeleton - is a property of the schema and not routing's."* Together they leave job 1 with no routing content, yet S1 and S2 (#84's job-1 scenarios per its body: *"the coordinator... can find anything once it understands the need"*) are the rows that produce *"chaining is missing"*. `0001`'s own ground for job 1 is the reload - *"it is interpreting a notice, not reading it, that forces a full reload"* - not reachability. The rewrite is authorized; this wording of it is the draft's. `repair`

11. **Attack 1 - §3's *"Every path carries the same shape and no depth"* is false for a repeat.** A `REPEAT` result holds paths of differing length for different members. `0039` permits asymmetry *"that comes from the material"*, so the symmetry claim survives on that clause, not *"by construction"* as §3 says. `repair`

12. **Attack 4 - §3's `0017` example is a path `0012` cannot produce.** *"a member reached over four links (`0017`'s deck citing the textbook four times) is four paths"*. Deck → textbook is artifact → artifact; `0012`'s nine rows have no such kind. `0017` supports the identity claim, not a routing instance. `repair`

13. **Attack 3 - §6 makes ring 0 a query result held resident and does not answer the landed reading that bars it.** §6: *"Ring 0 is one routing result, held resident by policy."* `0100`: *"`0043` bars a resident view from being a query"*; #84 and #82's Billy comment: *"a queried view cannot also be resident."* The draft's distinction - a system-held fixed standing intent, not a coordinator-issued parameterised query - threads `0043`'s own text, but it is never stated against that reading, and §8 #86's *"whether it is vacuous"* opens the standing predicate to job 3's join, which is what `0100`'s sentence explicitly refuses. `repair`

14. **Attack 4 - §1 and §8 decide what the tool description must carry, which is #79's.** §1: *"The vocabulary's closed sets... reach the coordinator through the tool description (#79)"*; §8: *"the closed sets the description must carry are the vocabulary §1 says spontaneity needs."* No landed record puts the kind set or `0012`'s table in a description; `0092` derives `has-more`'s values from it in-band. #79 is not in the protected list. `repair`

15. **Attack 4 - §9.3 amends `0060` and `0004` and leaves `0095` standing on the clause it amends.** `0095`: *"Anything reachable, one line, because `0060` fixes the cost of depth"* and *"Under this record the only unbounded thing in a return is the node itself... there is nothing for the conversation to fail to discard."* A resolve return is unbounded in width by §3 and §9.3. `0095` speaks of the block, so it is not contradicted, but its claim that `0043`'s discard is enforced by shape is now fetch-only and it gets no pointer. `repair`

16. **Attack 1 - §2's *join* and *crossing* are under-defined at the type level.** *"a chain used as a test on one member"* does not say what the test is (non-emptiness is §7's `exists`, a draft); and no sentence says what a crossing does to a member lacking the relation (`0046`: *"where no edge exists the walk returns nothing"*). The second decides whether `START(obligation) · MOVE(requires)` serves *what is owed* for obligations that require nothing. `repair`

## What did not break, per attack

**1 - types.** `CONTEXT.md`'s opening sentence decomposes into chains (obligation → `requires` → concept ← `covers` ← artifact) with all three positions on the path; job 3 is a start plus negated join; S1 by content is excluded by type and said so; S7's Ref-field crossing and the one-hop wildcard both fit the type; #86's size and absorption reach the vocabulary through annotation kinds; every path ends at a node so *address* is never ill-typed.

**2 - test.** With *other* excluded inside joins, no form admits itself through its own sub-chain; the stated limit covers `START(ref)` versus `START(K) · FILTER(id = X)`; under the exact reading the test generates `0003`'s `nodes_without`, which is #84's check on it; the second #16 test's promotion to a criterion is what #85's body asked for and its departure is declared.

**3 - derivation.** §3's paths are grounded top-down on `0001` job 2 via `0100`, not on any render; §2's vocabulary is drawn from `0012`, `0016`, `0017`, `0018`, `0027`, `0096`, none of which is a selection over ring 0; §5's commitment characterises the skeleton and does not derive a capability from `0059`'s tier.

**4 - scope and attribution.** Every cell of §5's deviation table verified against `0018`, `0017`, `0096`, `0012`, `0063`, `0029`, `0084`, `0021`, `0009`, `0027`, `0062`, `0019`, `0039`, `0043`, `0044` and `CONTEXT.md`'s `applies`; `0046`, `0081`, `0084`, `0094`, `0097`, `0098` and `0100` say what §1-§3 attribute to them; #87's ownership, #14's representation, #82's render, the grammar's spelling and by-handle's admission are each placed and not decided; §9.2's quotation of `0062` is verbatim.
