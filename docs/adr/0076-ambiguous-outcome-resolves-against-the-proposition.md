# An ambiguous outcome resolves against the proposition: "it basically round-trips" is a fail

The rejected alternative is the default - reading a partial pass as a pass. It is cheap to state and expensive in practice, and it is the rule that makes every other acceptance criterion mean anything.

Source: fall26:records/plan/application-tier.md §5
