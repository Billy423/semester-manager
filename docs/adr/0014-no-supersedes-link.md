# There is no `supersedes` link; revisions replace in place and are dated

No `supersedes` link kind exists. Every real revision replaced the file at the same path under the same name, so there is no prior version to point at; a `supersedes` link would mistype three shapes and hide a live document. Revision is carried by `revised_at`.

Source: fall26:records/domain/model.md §8
