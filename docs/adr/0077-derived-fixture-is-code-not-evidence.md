# A migrated fixture is a test input and therefore code; the original is never edited and stays as provenance

The rejected alternative was editing the original in place, which is invisible once taken. Duplication of identical rows is accepted in exchange for a provenance chain that survives migration.

Source: fall26:records/plan/application-tier.md §6
